<h1><%= @artist.id ? 'Edit Artist' : 'Add Artist' %></h1>
<%= form_for @artist.id ? Artist.find(params[:id]) : @artist do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit %>
<% end %>